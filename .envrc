#!/usr/bin/env bash

# Define direnv's `use devenv` integration before invoking it. The function is
# supplied by the installed devenv version, so it stays compatible with the
# project lock and does not require downloading an unpinned script.
eval "$(devenv direnvrc)"
use devenv
